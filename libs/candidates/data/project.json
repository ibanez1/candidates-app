{
  "name": "data",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/candidates/data/src",
  "prefix": "lib",
  "projectType": "library",
  "tags": ["scope:candidates", "type:data"],
  "targets": {
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "../../../coverage/libs/candidates/data"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
