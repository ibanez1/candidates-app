/* Setup for jest + Angular (jest-preset-angular v16+)
	Call the preset's zone test environment initializer. */
const { setupZoneTestEnv } = require('jest-preset-angular/setup-env/zone');

setupZoneTestEnv();

